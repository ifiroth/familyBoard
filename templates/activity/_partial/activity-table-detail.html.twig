<form action="{{ path('activity_edit', {'plannedActivity': activity.id }) }}" method="post")>
    <input type="text" value="{{ activity }}" name="activity_name">
</form>
<a href="{{ path('activity_close', {'plannedActivity': activity.id }) }}">Enregistrer et fermer</a>
