{% if activity is defined and activity.id in openedActivities|keys %}
    {% set form = openedActivities[activity.id].form %}
    {% set id = activity.id %}
{% else %}
    {% set form = emptyForm %}
    {% set id = null %}
{% endif %}

{{ form_start(form, {'action': path('activity_plan', {'id': id} ) } ) }}

{{ form_row(form) }}

<input type="submit" value="Enregistrer">

{{ form_end(form) }}
