{% if activity is defined and activity.id in forms|keys %}
    {% set form = forms[activity.id] %}
{% else %}
    {% set form = emptyForm %}
{% endif %}

{{ form_errors(form) }}

{{ form_start(form)}}

{{ form_row(form) }}

<input type="submit">

{{ form_end(form) }}
